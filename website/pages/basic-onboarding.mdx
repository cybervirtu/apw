import { Callout, Steps } from 'nextra/components'
import { PortalHero, PortalSectionIntro } from '../components/portal-ui'

# Basic Onboarding

<div className="apw-page">
  <PortalHero
    eyebrow="Level 1 — Start Now"
    title="The shortest safe beginner route into APW."
    lede="Use this page when you want a clear first-use path without hunting across the full docs set. It keeps setup, first use, and first-run guidance easy to find without flattening the real framework."
    actions={[
      { href: '/installation-guide', label: 'Install APW' },
      { href: '/basic-usage-guide', label: 'Basic Usage', variant: 'secondary' },
      { href: '/first-run-in-ide', label: 'First Run In IDE', variant: 'secondary' }
    ]}
    stats={[
      { value: 'Install', label: 'Set up APW locally and verify the wrapper works' },
      { value: 'Create', label: 'Make the downstream repo safely and initialize real state' },
      { value: 'Start', label: 'Open the project, read AGENTS.md, and choose the first workflow' }
    ]}
  />

  <Callout type="info" emoji="✅">
    Canonical source doc: <code>docs/BASIC_ONBOARDING_PROCEDURE.md</code>
  </Callout>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Who this is for"
      title="Beginner-safe by design."
      lede="This path is best for first-time APW users, non-technical or semi-technical builders, and anyone who wants the minimum safe route before touching deeper framework material."
    />
    <div className="apw-signal-grid">
      <div className="apw-signal-card">
        <h3>New to APW</h3>
        <p>You want a safe first-use path instead of a large documentation tree.</p>
      </div>
      <div className="apw-signal-card">
        <h3>Trying a real project</h3>
        <p>You want to create a project, initialize state, and begin with confidence.</p>
      </div>
      <div className="apw-signal-card">
        <h3>Trying to avoid framework overload</h3>
        <p>You want to know what matters now and what can wait until later.</p>
      </div>
    </div>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="What this path covers"
      title="The first-use flow, without the documentation dump."
      lede="Basic onboarding is designed to compress the first-use path into a sequence a beginner can actually follow."
    />
    <Steps>
    ### Understand APW in one minute

    Get the plain-English idea before touching deeper docs.

    ### Choose the project shape and profile

    Figure out what you are building and choose a likely APW profile.

    ### Bootstrap and validate

    Create the APW repo structure from anywhere in the workspace and let the wrapper validate it.

    ### Run guided project-state initialization

    Capture the project in plain language and generate the first core `.gsd` drafts.

    ### Start work safely

    Move into the downstream project, use the first-run flow, and begin with the right workflow.
    </Steps>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Read next"
      title="Take the next page that matches what is actually missing."
      lede="The point of this page is to reduce hunting, not create a second wall of links."
    />
    <div className="apw-next-strip">
      <a href="/installation-guide">Install APW first</a>
      <a href="/basic-usage-guide">Use APW after install</a>
      <a href="/first-run-in-ide">Open the project the right way</a>
      <a href="/get-started-flow">Let APW route you by situation</a>
    </div>
  </section>
</div>
