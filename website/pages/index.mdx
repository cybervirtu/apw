import { Callout, Cards } from 'nextra/components'
import { PortalHero, PortalPillRow, PortalSectionIntro } from '../components/portal-ui'

# APW Docs Portal

<div className="apw-home">
  <PortalHero
    eyebrow="Agentic Project Workspace"
    title="Build with AI without losing project state, trust, or momentum."
    lede="APW gives teams a governed project memory layer, a practical workflow layer, and a safer first-use path so AI-assisted work stays organized as the repo grows."
    actions={[
      { href: '/installation-guide', label: 'Start Now' },
      { href: '/what-is-apw', label: 'Learn How APW Works', variant: 'secondary' },
      { href: '/reference', label: 'Deep Reference', variant: 'secondary' }
    ]}
    stats={[
      { value: 'Level 1', label: 'Beginner-safe setup, first use, and first-run path' },
      { value: 'Level 2', label: 'Workflow, switching, persistence, and upgrade guidance' },
      { value: 'Level 3', label: 'Framework contract, compatibility, validation, and CI reference' }
    ]}
  />

  <div className="apw-note-grid">
    <div className="apw-trust-card">
      <h3>Serious framework, not prompt chaos</h3>
      <p>APW keeps project memory governed in <code>.gsd/</code>, execution support in <code>.agent/</code>, and official state changes deliberate.</p>
    </div>
    <div className="apw-trust-card">
      <h3>Beginner-safe entry</h3>
      <p>Installation, basic usage, first-run, and command guidance now form a lighter Level 1 path instead of a flat documentation dump.</p>
    </div>
    <div className="apw-trust-card">
      <h3>Project-centric day to day</h3>
      <p>Create from APW, work in the downstream project root, and use explicit helpers when you need to switch context safely.</p>
    </div>
    <div className="apw-trust-card">
      <h3>Safe framework evolution</h3>
      <p>Downstream projects can adopt newer APW-managed improvements through preview-first upgrade flow without blindly overwriting project-owned memory.</p>
    </div>
  </div>

  <Callout type="info" emoji="🧭">
    The portal is the polished presentation layer. The canonical APW rules still live in the repo-root governance files and the source docs under <code>docs/</code>.
  </Callout>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Start now"
      title="Choose the path that matches your moment."
      lede="APW should feel guided from the first click. Start with the setup and first-use path if you are new, then move into the deeper workflow and reference layers only when they are actually useful."
    />
    <Cards num={2}>
      <Cards.Card href="/installation-guide" title="Install APW">
        Set up APW locally, verify the wrapper works, and understand the minimum tools you need.
      </Cards.Card>
      <Cards.Card href="/basic-usage-guide" title="Basic Usage">
        Create your first project, initialize state, open the downstream repo, and start safely.
      </Cards.Card>
      <Cards.Card href="/first-run-in-ide" title="First Run In IDE">
        Open the right folder, read `AGENTS.md`, confirm `.gsd` state, and choose the first workflow.
      </Cards.Card>
      <Cards.Card href="/basic-onboarding" title="Basic Onboarding">
        Follow the shortest safe APW route if you want the broader beginner checklist around the same flow.
      </Cards.Card>
    </Cards>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="How APW works"
      title="One framework, three disciplined layers."
      lede="APW stays trustworthy because it separates governed memory, execution work, and official synchronization instead of letting every session rewrite the project story."
    />
    <div className="apw-journey-grid">
      <div className="apw-journey-step">
        <div className="apw-journey-step__index">1</div>
        <h3>GSD governs</h3>
        <p><code>.gsd/</code> holds the official memory: requirements, roadmap, state, tasks, evidence, and decisions.</p>
      </div>
      <div className="apw-journey-step">
        <div className="apw-journey-step__index">2</div>
        <h3>AGK executes</h3>
        <p><code>.agent/</code> provides workflows, agents, rules, and execution support for scoped implementation work.</p>
      </div>
      <div className="apw-journey-step">
        <div className="apw-journey-step__index">3</div>
        <h3>Evidence stays bounded</h3>
        <p>Useful execution output can be recorded safely without casually becoming the project’s official state.</p>
      </div>
      <div className="apw-journey-step">
        <div className="apw-journey-step__index">4</div>
        <h3>Orchestrator synchronizes</h3>
        <p>When canonical summaries need to change across files, orchestrator keeps those updates deliberate and consistent.</p>
      </div>
    </div>
    <PortalPillRow
      items={[
        'Governed project memory',
        'Action-first onboarding',
        'Preview-first upgrades',
        'Workflow persistence rules'
      ]}
    />
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Build with APW"
      title="Move beyond setup without falling into a wall of docs."
      lede="Once the repo exists, APW gives you better support for idea shaping, stack selection, switching, workflow choice, persistence, and upgrade. That guidance is there when you need it, not shoved at you too early."
    />
    <Cards num={2}>
      <Cards.Card href="/documentation-levels" title="Documentation Levels">
        See the full Start Now, Use APW Better, and Deep Reference model in one page.
      </Cards.Card>
      <Cards.Card href="/get-started-flow" title="Help Me Get Started">
        Let APW route you by situation instead of guessing which guide matters first.
      </Cards.Card>
      <Cards.Card href="/idea-to-project" title="Idea To Project">
        Turn a rough idea into a structured project path before implementation gets messy.
      </Cards.Card>
      <Cards.Card href="/commands-and-workflows" title="Commands & Workflows">
        Learn the operator layer for `/status`, `/brainstorm`, `/create`, `/debug`, `/test`, and `/orchestrate`.
      </Cards.Card>
      <Cards.Card href="/safe-context-switching" title="Safe Context Switching">
        Recover orientation and move between APW root, downstream projects, and the workspace parent without silent context changes.
      </Cards.Card>
      <Cards.Card href="/downstream-project-upgrade" title="Project Upgrade">
        Refresh APW-managed files in an existing downstream repo through the safe preview-first upgrade flow.
      </Cards.Card>
    </Cards>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Why teams trust it"
      title="Professional guardrails, not fake polish."
      lede="APW is designed to feel credible because the operating model is explicit: project work belongs in the downstream repo, evidence is bounded, and framework evolution is controlled."
    />
    <div className="apw-signal-grid">
      <div className="apw-signal-card">
        <h3>Clear operating model</h3>
        <p>Users know where APW root lives, where downstream work belongs, and where workspace helpers make sense.</p>
      </div>
      <div className="apw-signal-card">
        <h3>Safe persistence rules</h3>
        <p>Workflow output is not canonical by default. Evidence is saved safely first, then promoted deliberately.</p>
      </div>
      <div className="apw-signal-card">
        <h3>Upgrade boundaries that make sense</h3>
        <p>APW-managed files can refresh, while project-owned `.gsd` memory and implementation files stay protected.</p>
      </div>
      <div className="apw-signal-card">
        <h3>Layered documentation</h3>
        <p>Beginners get a lighter path, active users get better guidance, and maintainers still have the full reference layer.</p>
      </div>
    </div>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Deep reference"
      title="Reach the contract only when you need the contract."
      lede="The deeper governance, compatibility, validation, and CI material is still there. It just no longer dominates the first impression."
    />
    <Cards num={2}>
      <Cards.Card href="/reference" title="Reference">
        Reach the deeper contract, compatibility, validation, and CI material when you need the full source.
      </Cards.Card>
      <Cards.Card href="/what-is-apw" title="What Is APW?">
        Read the plain-English explanation first if you want the model without jumping straight into rules and internals.
      </Cards.Card>
    </Cards>
  </section>
</div>
