import { Callout, Cards, Steps } from 'nextra/components'

# Commands & Workflows

This is the operator layer for APW.

This page is a portal summary for operators. The detailed workflow rules and command behavior remain canonical in the repo docs it wraps.

<Callout type="warning" emoji="🎯">
Use a specific workflow for a specific job. In APW, vague prompting is weaker than routing work through the right agent and workflow pair.
</Callout>

<Callout type="info" emoji="🧱">
If the repo is brand new, run the repo's `docs/GUIDED_PROJECT_STATE_INITIALIZATION.md` flow first so the workflows have real `.gsd` context to read.
</Callout>

<Callout type="info" emoji="📍">
These workflows belong in the downstream project root, not APW root and not the workspace parent folder.
</Callout>

## The practical invocation pattern

```text
@agent /workflow task description
```

Examples:

```text
@orchestrator /status summarize the current state and next best action
@backend-specialist /create implement password reset API from TODO.md
@qa-automation-engineer /test verify checkout integration flow
```

## Choose the workflow by intent

<Cards num={2}>
  <Cards.Card href="/get-started-flow" title="I Need To Re-orient">
    Use `/status` if you are returning after time away or need a quick project briefing.
  </Cards.Card>
  <Cards.Card href="/commands-and-workflows#quick-chooser" title="I Need To Shape An Idea">
    Use `/brainstorm` before implementation when the idea still needs structure.
  </Cards.Card>
  <Cards.Card href="/commands-and-workflows#quick-chooser" title="I Need To Build Something New">
    Use `/create` for new feature or file work tied to requirements and TODOs.
  </Cards.Card>
  <Cards.Card href="/commands-and-workflows#quick-chooser" title="I Need To Improve Existing Code">
    Use `/enhance` when behavior should stay the same but the code needs cleanup or refinement.
  </Cards.Card>
  <Cards.Card href="/commands-and-workflows#quick-chooser" title="Something Is Broken">
    Use `/debug` for diagnosis and safe bug fixing without unnecessary rewrites.
  </Cards.Card>
  <Cards.Card href="/commands-and-workflows#quick-chooser" title="The Task Is Too Large">
    Use `/orchestrate` when work crosses modules, files, or specialties and needs decomposition first.
  </Cards.Card>
</Cards>

## Quick chooser

| Situation | Workflow |
| --- | --- |
| returning after a break | `/status` |
| shaping an idea before implementation | `/brainstorm` |
| building something new | `/create` |
| improving existing code | `/enhance` |
| fixing a bug | `/debug` |
| verifying quality or completion | `/test` |
| planning frontend structure | `/design` |
| polishing UI quality | `/ui-ux-pro-max` |
| preparing local review | `/preview` |
| preparing release or deployment | `/deploy` |
| decomposing a large cross-cutting task | `/orchestrate` |

## The APW execution rule

<Steps>
### Read context first

Start from `AGENTS.md`, then read the minimum required `.gsd` context for the task.

### Do scoped work

Use the workflow that matches the job instead of issuing a vague prompt.

### Keep evidence bounded

Execution agents may append bounded evidence to `.gsd/JOURNAL.md`.

### Let the orchestrator synchronize official state

Execution agents should not freely rewrite `STATE.md`, `ROADMAP.md`, `TODO.md`, or `DECISIONS.md`.
</Steps>

## The three key operator docs

| Source doc | What it is for |
| --- | --- |
| `docs/WORKFLOW_SELECTION_GUIDE.md` | choosing the right workflow for the current problem |
| `docs/COMMAND_INVOCATION_GUIDE.md` | read-first context, expected outputs, and handoff rules |
| `docs/AGENT_PLUS_WORKFLOW_EXAMPLES.md` | real invocation patterns and good agent-workflow pairings |

## Canonical source docs

This page wraps:

- `docs/WORKFLOW_SELECTION_GUIDE.md`
- `docs/COMMAND_INVOCATION_GUIDE.md`
- `docs/AGENT_PLUS_WORKFLOW_EXAMPLES.md`
- `docs/TOOLING_GUIDE.md`
- `docs/DOCS_SOURCE_OF_TRUTH.md`

## What to do next

- Go to [Reference](/reference) if you need the deeper governance, compatibility, validation, or CI documentation behind the workflow layer.
- Go to [Team / Migration / Adoption](/team-migration-and-adoption) if you are introducing APW to a team or existing repo.
