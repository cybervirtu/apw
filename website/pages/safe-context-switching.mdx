import { Callout, Steps } from 'nextra/components'
import { PortalHero, PortalSectionIntro } from '../components/portal-ui'

# Safe Context Switching

<div className="apw-page">
  <PortalHero
    eyebrow="Safe Switching"
    title="Move between APW root, projects, and the workspace parent without guesswork."
    lede="APW helps you switch explicitly and visibly. It does not hide context changes, and it does not blur the difference between framework work and project work."
    actions={[
      { href: '/first-run-in-ide', label: 'First Run In IDE' },
      { href: '/documentation-levels', label: 'See The Reading Levels', variant: 'secondary' }
    ]}
    stats={[
      { value: 'APW root', label: 'Framework maintenance and shared APW behavior' },
      { value: 'Project root', label: 'Normal day-to-day implementation work' },
      { value: 'Workspace parent', label: 'Organizer and launch point, not the real work site' }
    ]}
  />

  <Callout type="info" emoji="🔀">
    Canonical source doc: <code>docs/SAFE_CONTEXT_SWITCHING.md</code>
  </Callout>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="What the helpers are for"
      title="Use switching to recover orientation, not to hide it."
      lede="The helpers answer the practical questions people actually ask when working across multiple repos."
    />
    <div className="apw-signal-grid">
      <div className="apw-signal-card">
        <h3>Where am I right now?</h3>
        <p>Use context detection instead of assuming a folder is the right repo because it looks familiar.</p>
      </div>
      <div className="apw-signal-card">
        <h3>What projects are here?</h3>
        <p>List recognized downstream projects instead of manually hunting the workspace tree.</p>
      </div>
      <div className="apw-signal-card">
        <h3>How do I get back to the right place?</h3>
        <p>Switch explicitly to framework, project, or parent and keep the destination visible to the user.</p>
      </div>
    </div>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Practical helper flow"
      title="Use the helpers in a calm, visible sequence."
      lede="APW switching is intentionally boring: resolve, classify, confirm, and keep going in the right repo."
    />
    <Steps>
    ### Check your current location

    Run `apw context`.

    ### See the known projects

    Run `apw list-projects`.

    ### Switch explicitly

    Run `apw switch framework`, `apw switch <name>`, or `apw switch parent`.

    ### Work in the right place

    Once you are in the downstream project root, open `AGENTS.md` and continue normal project work there.
    </Steps>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Read next"
      title="Take the next page that reduces confusion."
      lede="Most users only need one more context page after this."
    />
    <div className="apw-next-strip">
      <a href="/first-run-in-ide">Open the project correctly in the IDE</a>
      <a href="/commands-and-workflows">Run the right workflow next</a>
      <a href="/start-here">Return to the beginner path</a>
    </div>
  </section>
</div>
