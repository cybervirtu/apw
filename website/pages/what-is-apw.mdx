import { Callout, Steps } from 'nextra/components'
import { PortalHero, PortalPillRow, PortalSectionIntro } from '../components/portal-ui'

# What Is APW?

<div className="apw-page">
  <PortalHero
    eyebrow="Plain-English Explanation"
    title="APW is a framework for AI-assisted projects that need to stay understandable over time."
    lede="It gives the project a governed memory layer, a practical execution layer, and a controlled way to keep official state in sync as implementation moves forward."
    actions={[
      { href: '/basic-usage-guide', label: 'Start Using APW' },
      { href: '/idea-to-project', label: 'See The Project Journey', variant: 'secondary' }
    ]}
    stats={[
      { value: '.gsd', label: 'Governed project memory and official state' },
      { value: '.agent', label: 'Execution workflows, rules, agents, and skills' },
      { value: 'Orchestrator', label: 'Deliberate synchronization for canonical summaries' }
    ]}
  />

  <Callout type="info" emoji="📘">
    This is a beginner-friendly explanation. The canonical APW contract still lives in the repo-root governance files and the deeper source docs under <code>docs/</code>.
  </Callout>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Why APW matters"
      title="AI sessions move fast. Project truth should not drift with them."
      lede="APW prevents the common failure mode where one good session turns into a repo full of unclear state, conflicting notes, and undocumented decisions."
    />
    <div className="apw-signal-grid">
      <div className="apw-signal-card">
        <h3>The project gets a memory</h3>
        <p>APW gives the repo a trusted place to hold requirements, state, tasks, decisions, and evidence.</p>
      </div>
      <div className="apw-signal-card">
        <h3>Agents get a disciplined workspace</h3>
        <p>Instead of improvising context every time, execution tools work inside governed scope and reusable workflows.</p>
      </div>
      <div className="apw-signal-card">
        <h3>The team keeps alignment</h3>
        <p>Canonical summaries do not get rewritten casually. Important state changes stay controlled and explainable.</p>
      </div>
    </div>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="The APW model"
      title="Three roles, one operating system."
      lede="APW works because it separates what the project officially knows, how work gets executed, and how official summaries are synchronized."
    />
    <div className="apw-table-wrap">
      <table>
        <thead>
          <tr>
            <th>Part</th>
            <th>Layer</th>
            <th>Responsibility</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><strong>GSD</strong></td>
            <td><code>.gsd/</code></td>
            <td>project memory, lifecycle, roadmap, and canonical state</td>
          </tr>
          <tr>
            <td><strong>AGK</strong></td>
            <td><code>.agent/</code></td>
            <td>execution rules, workflows, specialists, and skills</td>
          </tr>
          <tr>
            <td><strong>Orchestrator</strong></td>
            <td>sync layer</td>
            <td>updates official project summaries when the canonical state needs to change</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="The team model"
      title="Think of APW like a disciplined project team."
      lede="This mental model is easier to remember than raw framework vocabulary."
    />
    <Steps>
    ### GSD governs

    GSD owns lifecycle, verification, and canonical project memory.

    ### AGK executes

    AGK provides the specialist execution help that builds, debugs, tests, and improves the project.

    ### The orchestrator synchronizes

    The orchestrator reads execution results and updates canonical state when the official summary needs to change.
    </Steps>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="What APW helps you do"
      title="Replace prompt chaos with a credible operating model."
      lede="The framework is most valuable when the project lasts longer than a single burst of coding."
    />
    <PortalPillRow
      items={[
        'Start with structure',
        'Keep sessions aligned',
        'Separate evidence from official status',
        'Hand work across sessions safely',
        'Validate the repo over time'
      ]}
    />
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="What to do next"
      title="Keep moving without over-reading."
      lede="Once the model makes sense, take the next page that matches your actual need."
    />
    <div className="apw-next-strip">
      <a href="/basic-usage-guide">Start using APW</a>
      <a href="/idea-to-project">See how an idea becomes a project</a>
      <a href="/profiles-and-stack-selection">Choose a likely direction</a>
      <a href="/reference">Reach the deeper contract</a>
    </div>
  </section>
</div>
