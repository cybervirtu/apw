import { Callout, Steps } from 'nextra/components'
import { PortalHero, PortalPillRow, PortalSectionIntro } from '../components/portal-ui'

# Workflow Persistence Policy

<div className="apw-page">
  <PortalHero
    eyebrow="Persistence Model"
    title="Workflow output is useful. Official state is still deliberate."
    lede="APW keeps a consistent rule across the core workflows: execution output does not become canonical project memory automatically. Evidence is saved safely first, then promoted on purpose."
    actions={[
      { href: '/commands-and-workflows', label: 'Explore Workflows' },
      { href: '/chat-first-requirement-ingestion', label: 'Requirement Ingestion', variant: 'secondary' },
      { href: '/chat-requirement-persistence', label: 'Persistence Choices', variant: 'secondary' },
      { href: '/brainstorm-persistence-and-promotion', label: 'Brainstorm Persistence', variant: 'secondary' }
    ]}
    stats={[
      { value: 'Default', label: 'Bounded evidence goes to JOURNAL.md first' },
      { value: 'Promotion', label: 'Tasks, status, roadmap, and decisions move deliberately' },
      { value: 'Orchestrator', label: 'Owns official cross-file synchronization' }
    ]}
  />

  <Callout type="info" emoji="📝">
    Canonical source doc: <code>docs/WORKFLOW_PERSISTENCE_POLICY.md</code>
  </Callout>

  <Callout type="info" emoji="💬">
    If the important project input arrived through plain chat before a workflow even ran, follow the chat-first ingestion model first.
  </Callout>

  <Callout type="info" emoji="📦">
    After that, APW should still tell the user whether the result was saved as notes, promoted directly, handed to orchestrator, or not saved yet.
  </Callout>

  <PortalPillRow
    items={[
      'Execution output is not canonical by default',
      'JOURNAL.md is the safe save path',
      'One slice should close before the next one opens',
      'Promotion is deliberate',
      'Orchestrator owns cross-file sync'
    ]}
  />

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="The practical model"
      title="Five moves keep workflow output trustworthy."
      lede="This is the APW-wide pattern behind `/status`, `/create`, `/enhance`, `/debug`, `/test`, and `/orchestrate`."
    />
    <Steps>
    ### Run the workflow

    Use `/status`, `/create`, `/enhance`, `/debug`, `/test`, or `/orchestrate` for the actual job.

    ### Keep the current slice explicit

    If the task is large, break it down first. APW should not quietly roll several implementation slices into one vague pass.

    ### Keep the useful evidence

    Do not leave the useful result floating only in chat or terminal output.

    ### Save it safely

    Use `JOURNAL.md` as the default evidence layer.

    ### Promote only what became official

    Move tasks, status, roadmap changes, or rationale into canonical `.gsd` files deliberately.

    ### Use orchestrator when project memory changes across files

    Persist evidence first, then let orchestrator synchronize the official state.
    </Steps>
  </section>

  <section className="apw-section">
    <PortalSectionIntro
      eyebrow="Read next"
      title="Use the source doc that matches the question."
      lede="Most users need one follow-up page: either the full workflow policy, command details, or examples."
    />
    <div className="apw-next-strip">
      <a href="/chat-first-requirement-ingestion">Follow chat-first ingestion</a>
      <a href="/chat-requirements-flow">Follow the full chat flow</a>
      <a href="/chat-requirement-persistence">Follow persistence choices</a>
      <a href="/atomic-implementation-planning">Follow atomic planning</a>
      <a href="/commands-and-workflows">See the operator layer</a>
      <a href="/brainstorm-persistence-and-promotion">Follow brainstorm persistence</a>
      <a href="/reference">Reach deeper reference</a>
    </div>
  </section>
</div>
